// Importamos las funciones toRandomCase y vowelsToNumbers de los archivos correspondientes
const toRandomCase = require("./toRandomCase")
const vowelsToNumbers = require("./vowelsToNumbers")

// Exportamos las funciones para que puedan ser utilizadas en otros archivos
module.exports = { toRandomCase, vowelsToNumbers }

