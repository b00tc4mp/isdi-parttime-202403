function splice (array, indexStart, deleteElement) {

    for (indexStart; indexStart < array.length; indexStart++) {

        var element = array[indexStart]

        

    }

}




if(indexStart < 0) indexStart = array.length + indexStart