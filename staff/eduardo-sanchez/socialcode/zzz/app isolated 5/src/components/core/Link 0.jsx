function Link({ onClick, children }) {
    return <a href="" onClick={onClick}>{children}</a>
}

export default Link

// esta seria la siguiente iteracion 

/*
export default function Link({ onClick, children }) {
    return <a href="" onClick={onClick}>{children}</a>
}
*/